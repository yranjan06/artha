version: "3.8"

services:
  artha:
    build: .
    ports:
      - "7860:7860"
    env_file: .env
    volumes:
      - ./memory:/app/memory   # persist user data across restarts
    restart: unless-stopped
